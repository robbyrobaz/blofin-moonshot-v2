# Moonshot Strategy Ideas — 2026-03-24

## Context
- **Current champion:** de44f72dbb01 (short, +0.68% PnL, PF 2.18, 388 trades, "all" features)
- **Backtest queue:** 0 models pending
- **Recent performance:** 96% backtest failure rate (2435/2541 retired in 30d)
- **Top failure reason:** backtest_failed (95.8%)
- **Available features:** 50 total (25 core + 12 extended + 13 social)

## Analysis

### What's Working
1. **"all" feature set** — champion uses full 50-feature set
2. **Short direction** — current champion is short-only
3. **High trade count** — 388 trades = strong statistical confidence

### Identified Gaps
1. **No long champion** — only short working (new_listing champion has 0 trades)
2. **Feature utilization** — need more feature subset experiments
3. **Lookback diversity** — all features use same 52w window, could test 30d/90d variants
4. **Regime specialization** — no models targeting high-vol vs low-vol regimes
5. **Model type diversity** — need distribution check (XGBoost vs LightGBM vs CatBoost)

## Proposed Variants (7 models)

### 1. Momentum Specialist (Short)
**Goal:** Test if pure momentum + volume beats "all" features  
**Features:** momentum_4w, momentum_8w, volume_ratio_7d, volume_spike, obv_slope, btc_30d_return  
**Params:** XGBoost, depth=6, lr=0.05, threshold=0.50, neg_weight=5  
**Why:** Champion proves short works — test minimal feature hypothesis

### 2. Volatility Breakout (Long)
**Goal:** Find long champion via volatility expansion signals  
**Features:** atr_compression, bb_squeeze_pct, high_low_range_pct, volume_spike, oi_change_24h  
**Params:** LightGBM, depth=8, lr=0.01, threshold=0.40, neg_weight=8  
**Why:** Longs need volatility expansion to work in crypto

### 3. Social Sentiment Boost (Short)
**Goal:** Test if social features improve short timing  
**Features:** core_only + social_features (no extended)  
**Params:** CatBoost, depth=6, lr=0.05, threshold=0.60, neg_weight=5  
**Why:** Social panic could signal short entries

### 4. High-Confidence Filter (Short)
**Goal:** Fewer trades, higher win rate via strict threshold  
**Features:** all  
**Params:** XGBoost, depth=10, lr=0.01, threshold=0.80, neg_weight=3  
**Why:** Current champion might be overtrading — test quality over quantity

### 5. Price Structure Expert (Long)
**Goal:** Support/resistance + structure for long entries  
**Features:** distance_from_support, distance_from_resistance, higher_highs, consec_up_bars, price_vs_52w_low, volume_ratio_3d  
**Params:** LightGBM, depth=4, lr=0.1, threshold=0.40, neg_weight=10  
**Why:** Longs need clear structure reversals

### 6. OI Divergence Hunter (Short)
**Goal:** Open interest divergence as short signal  
**Features:** oi_price_divergence, oi_change_7d, oi_percentile_90d, funding_rate_extreme, price_vs_24h_high, momentum_4w  
**Params:** CatBoost, depth=8, lr=0.05, threshold=0.50, neg_weight=5  
**Why:** OI/price divergence is classic short setup

### 7. Regime-Aware Ensemble (Short)
**Goal:** BTC regime filters + alt momentum  
**Features:** btc_30d_return, btc_vol_percentile, market_breadth, momentum_4w, volume_ratio_7d, atr_percentile  
**Params:** XGBoost, depth=6, lr=0.05, threshold=0.60, neg_weight=5  
**Why:** Market-wide regime should filter coin-specific signals

## Implementation Plan
1. Generate 7 model configs (JSON format)
2. Insert into tournament_models (stage='backtest_pending')
3. Next cycle will backtest them
4. Track feature subset performance in dashboard

## Success Metrics
- At least 1 long model reaches FT (currently 0)
- Feature subset analysis: which combinations pass BT gates?
- If any variant beats champion PnL → automatic promotion
